version: '3.8'

services:
  app:
    build:
      context: .
      target: development
    ports:
      - "3333:3333"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - PORT=3333
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
